#!/usr/bin/python3
"""
Type-annotated function concat that takes
two strings and returns a concatenated string.
"""


def concat(str1: str, str2: str) -> str:
    """Concatenates two strings."""

    str = str1 + str2
    return str
